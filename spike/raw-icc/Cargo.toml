# THROWAWAY. Outside the workspace, like the other spikes.
#
# PLAN 0.6. RAW decode and ICC transform feasibility -- pure-Rust vs FFI,
# and the LGPL dynamic-linking question (PRD §14) if FFI is chosen.
[package]
name = "raw-icc"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
rawler = "0.7.2"
lcms2 = "6.1.1"
moxcms = { version = "0.8.1", features = ["extended_range"] }
